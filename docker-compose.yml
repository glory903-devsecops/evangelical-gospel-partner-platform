version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evangelical_gospel_partner_web
    ports:
      - "5000:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
